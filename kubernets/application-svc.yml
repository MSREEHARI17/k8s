# Node Port Service
apiVersion: v1
kind: Service
metadata:
  namespace: spring-boot-app
  name: springappsvc
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: springapp
